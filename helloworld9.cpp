// math related stuff

#include <iostream> 
#include <cmath>

int main() {

    double x = 3.94;
    double y = 4;
    double z;

    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = pow(5, 3);
    //z = sqrt(9);
    //z = abs(-3);
    //z = floor(x);

    std::cout << z;

    return 0;
}